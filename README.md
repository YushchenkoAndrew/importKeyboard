# Import Keyboard

It's a fun project created only for using my laptop keyboard for manipulating Raspberry Pi GNU

### Run Server side:

```
  npm run server
```

### Run Clint side:

```
  npm run clint
```
